%% PENDANTS (0.75,2)

p = 0.75 ; q = 2;
dataset = 'A'; noise_ratio = 0.005; dataId = [dataset, num2str(noise_ratio*100)]; 
main_grid_hyperparam_PENDANTS;

disp(['Simulation of with ',dataId , ' is accomplished'])

% dataset = 'A'; noise_ratio = 0.01; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_PENDANTS;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
% 
% dataset = 'A'; noise_ratio = 0.02; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_PENDANTS;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
clearvars -except p q;

dataset = 'B'; noise_ratio = 0.005; dataId = [dataset, num2str(noise_ratio*100)]; 
main_grid_hyperparam_PENDANTS;

disp(['Simulation of with ',dataId , ' is accomplished'])

clearvars -except p q;

% dataset = 'B'; noise_ratio = 0.01; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_PENDANTS;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
% 
% dataset = 'B'; noise_ratio = 0.02; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_PENDANTS;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% clearvars -except p q;

%% PENDANTS (1,2)

p =1; q = 2;
dataset = 'A'; noise_ratio = 0.005; dataId = [dataset, num2str(noise_ratio*100)]; 
main_grid_hyperparam_PENDANTS;

disp(['Simulation of with ',dataId , ' is accomplished'])

clearvars -except p q;

% dataset = 'A'; noise_ratio = 0.01; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_PENDANTS;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
% 
% dataset = 'A'; noise_ratio = 0.02; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_PENDANTS;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
dataset = 'B'; noise_ratio = 0.005; dataId = [dataset, num2str(noise_ratio*100)]; 
main_grid_hyperparam_PENDANTS;

disp(['Simulation of with ',dataId , ' is accomplished'])

clearvars -except p q;

% dataset = 'B'; noise_ratio = 0.01; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_PENDANTS;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
% 
% dataset = 'B'; noise_ratio = 0.02; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_PENDANTS;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% clearvars -except p q;

%% backcorSPOQ (0.75,2)

p = 0.75 ; q = 2;
dataset = 'A'; noise_ratio = 0.005; dataId = [dataset, num2str(noise_ratio*100)]; 
main_grid_hyperparam_backcorSPOQ;

disp(['Simulation of with ',dataId , ' is accomplished'])

clearvars -except p q;



% dataset = 'A'; noise_ratio = 0.01; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_backcorSPOQ;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
% 
% dataset = 'A'; noise_ratio = 0.02; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_backcorSPOQ;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
dataset = 'B'; noise_ratio = 0.005; dataId = [dataset, num2str(noise_ratio*100)]; 
main_grid_hyperparam_backcorSPOQ;

disp(['Simulation of with ',dataId , ' is accomplished'])

clearvars -except p q;


% dataset = 'B'; noise_ratio = 0.01; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_backcorSPOQ;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
% 
% dataset = 'B'; noise_ratio = 0.02; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_backcorSPOQ;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% clearvars -except p q;

%% backcorSPOQ (1,2)

p =1; q = 2;
dataset = 'A'; noise_ratio = 0.005; dataId = [dataset, num2str(noise_ratio*100)]; 
main_grid_hyperparam_backcorSPOQ;

disp(['Simulation of with ',dataId , ' is accomplished'])

clearvars -except p q;

% dataset = 'A'; noise_ratio = 0.01; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_backcorSPOQ;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
% 
% dataset = 'A'; noise_ratio = 0.02; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_backcorSPOQ;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;

dataset = 'B'; noise_ratio = 0.005; dataId = [dataset, num2str(noise_ratio*100)]; 
main_grid_hyperparam_backcorSPOQ;

disp(['Simulation of with ',dataId , ' is accomplished'])

clearvars -except p q;

% dataset = 'B'; noise_ratio = 0.01; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_backcorSPOQ;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% 
% clearvars -except p q;
% 
% dataset = 'B'; noise_ratio = 0.02; dataId = [dataset, num2str(noise_ratio*100)]; 
% main_grid_hyperparam_backcorSPOQ;
% 
% disp(['Simulation of with ',dataId , ' is accomplished'])
% clearvars -except p q;